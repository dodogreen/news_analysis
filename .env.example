# ===========================================================================
# 機密設定檔 — 請複製為 .env 並填入你的真實資訊
# cp .env.example .env && vi .env
# ===========================================================================

# --- Google Gemini API ---
# 型別: string（從 https://aistudio.google.com/apikey 取得）
GEMINI_API_KEY=your_gemini_api_key_here

# --- NewsAPI.org ---
# 型別: string（從 https://newsapi.org/register 取得，免費方案每日 100 次請求）
NEWSAPI_KEY=your_newsapi_key_here

# --- SMTP 郵件寄送（寄件者帳號設定） ---
# 注意：這裡只設定「誰來寄信」，「寄給誰」請在 config.yaml 的 email.recipients 設定
#
# SMTP_HOST: string — SMTP 伺服器位址（Gmail 用 smtp.gmail.com）
SMTP_HOST=smtp.gmail.com

# SMTP_PORT: integer — SMTP 連接埠（TLS 用 587，SSL 用 465）
SMTP_PORT=587

# SMTP_USER: string — 寄件者的 Gmail 帳號（用來登入 SMTP）
SMTP_USER=your_email@gmail.com

# SMTP_PASSWORD: string — Gmail 應用程式密碼（16 碼，非一般登入密碼）
# 取得方式: Google 帳戶 → 安全性 → 兩步驟驗證 → 應用程式密碼
# 格式範例: abcd efgh ijkl mnop
SMTP_PASSWORD=your_gmail_app_password

# EMAIL_FROM: string — 寄件者顯示地址（通常與 SMTP_USER 相同）
EMAIL_FROM=your_email@gmail.com
